enum NotificationType {
  message,
  connectionRequest,
  application,
  takeRequestConfirmation,
  userCompletedJob,
  posterCompletedJob,
  jobCancellation,
  dev
}

enum NotificationStatus {
  unread,
  read,
  clicked,
}

class Notification {
  
}
